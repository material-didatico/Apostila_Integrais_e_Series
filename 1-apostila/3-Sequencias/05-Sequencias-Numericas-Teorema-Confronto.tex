%------------------------------------------------------------------------------%
\section{Teorema do Confronto}

O Teorema do Confronto é um resultado que nos permite
obter o limite de uma sequência pela análise de sequências outras mais simples,
ou já conhecidas, como descrito no teorema a seguir.

%------------------------------------------------------------------------------%
\begin{teorema}{Teorema do Confronto para Sequências}{teo:confronto}
  Sejam\index{Teorema!confronto} $(a_n)$, $(b_n)$ e $(c_n)$
  sequências de números reais tais que, para $n\in\N$,
  \[
    a_n \leq b_n \leq c_n  \,.
  \]
  Se as sequências $(a_n)$ e $(c_n)$ convergem para um mesmo limite $L$
  \[
    \lim a_n = \lim c_n = L
  \]
  então a sequência $b_n$ é convergente e
  \[
    \lim b_n = L \,.
  \]
\end{teorema}
%------------------------------------------------------------------------------%
\begin{proof}
  Como
  \[
    \lim a_n=\lim c_n=L  \,,
  \]
  dado $\varepsilon>0$, existe $N\in \N$, por exemplo, $N=\max(N_1, N_2)$,
  onde $N_1$ e $N_2$ são associados às sequências $a_n$ e $c_n$,
  tal que, para todo $n>N$
  \[
    \abs{a_n-L} < \varepsilon
    \qquad\text{e}\qquad
    \abs{c_n-L} < \varepsilon \,,
  \]
  ou seja,
  \[
    -\varepsilon+L < a_n < \varepsilon+L
    \qquad\text{e}\qquad
    -\varepsilon+L < c_n < \varepsilon+L   \,,
  \]
  mas como $a_n\leq b_n\leq c_n$, segue que
  \[
    -\varepsilon+L \;<\; a_n \;\leq\; b_n \;\leq\; c_n \;<\; \varepsilon+L   \,.
  \]
  Daí seque que $-\varepsilon <b_n-L<\varepsilon$,
  ou seja $\abs{b_n-L} < \varepsilon$, o que implica que $b_n\to L$.
\end{proof}
%------------------------------------------------------------------------------%

A Figura~\ref{fig:teorema_confronto} ilustra o
Teorema do Confronto~\ref{teo:confronto}, no caso
onde sabemos que $a_n$ e $c_n$ convergem
para $L$ e que $a_n \leq b_n \leq c_n$,
portanto podemos concluir que $b_n\to L$.

%------------------------------------------------------------------------------%
\begin{figure}[h]
  \centering
  \input{fig_teorema_confronto}
  \caption{Ilustração do Teorema do Confronto com $a_n \leq b_n \leq c_n$.}
  \label{fig:teorema_confronto}
\end{figure}
%------------------------------------------------------------------------------%

O próximo exemplo mostra uma aplicação do Teorema do Confronto para calcular
o limite de uma sequência.

%------------------------------------------------------------------------------%
\begin{example}
  Calcule o limite da sequência
  \(
    a_n=\frac{(-1)^{n+1}}{2n-1}
  \).
  \solution
  Note que
  \[
    \frac{-1}{2n-1} \leq \frac{(-1)^{n+1}}{2n-1} \leq \frac{1}{2n-1}   \,.
  \]
  Como
  \[
    \lim\frac{-1}{2n-1} = \lim \frac{1}{2n-1} = 0  \,,
  \]
  segue, pelo Teorema do Confronto, que
  \[
    \lim \frac{(-1)^{n+1}}{2n-1} = 0   \,.
  \]
\end{example}
%------------------------------------------------------------------------------%

Analisando a sequência do exemplo anterior observamos que ela é o produto de
uma sequência limitada
\[
  a_n = (-1)^{n+1}
\]
por uma sequência que converge para zero
\[
  b_n = \sfrac{1}{(2n-1)}
\]
e concluímos que ela converge para zero.
Esse fato é verdade, como apresentado pelo corolário a seguir.

%------------------------------------------------------------------------------%
\begin{corolario}{Sequência que Converge para Zero}{cor:produto-zero}
  Se $a_n$ é uma sequência limitada
  \[
    \abs{a_n} \leq M
  \]
  para todo $n\in\N$ e algum número $M>0$ e $b_n$ uma sequência que
  converge para zero
  \[
    \lim b_n = 0
  \]
  então
  \[
    \lim a_n\, b_n = 0  \,.
  \]
\end{corolario}
%------------------------------------------------------------------------------%

O próximo exemplo utiliza esse resultado para calcular o limite de uma sequência.

%------------------------------------------------------------------------------%
\begin{example}
  Calcular o limite de
  \(
    \frac{\cos n}{n} \,.
  \)
  \solution
  Uma vez que $\cos n$ é limitada e
  \(
    \frac{1}{n} \to 0
  \)
  podemos concluir que
  \(
    \lim \frac{\cos n}{n} = 0
  \).
%------------------------------------------------------------------------------%
\end{example}

%------------------------------------------------------------------------------%
